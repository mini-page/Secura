# Secura Web

Lightweight React web client for the Secura Django API.

## Setup

```bash
cd web
npm install
npm run dev
```

## API

By default this points to `http://localhost:4000`.
Update in `web/src/api/client.js` if needed.
